import React from "react"

type SliderProps = {
    children: React.ReactNode
}

export const Slider = ({ children }: SliderProps ) => {
    return(
        <>
            {children}
        </>
    )
}